import 'package:flutter/material.dart';

class AllScreenWidget extends StatelessWidget {
  const AllScreenWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ALl Screen Widget"),
      ),
    );
  }
}