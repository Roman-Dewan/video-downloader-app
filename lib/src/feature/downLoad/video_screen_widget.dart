import 'package:flutter/material.dart';

class VideoScreenWidget extends StatelessWidget {
  const VideoScreenWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Video Screen Widget"),
      ),
    );
  }
}